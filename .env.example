# Opcional: um token pessoal do GitHub (sem escopos, "public access") sobe o
# limite da API de 60 para 5000 req/h. Sem ele o site funciona igual — o cache
# de 1 hora segura o limite não autenticado.
GITHUB_TOKEN=
